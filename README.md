# Cypress Login Automation Test Project

This is a simple Cypress project to test login features using OrangeHRM demo site.

## Install & Run

```bash
npm install
npx cypress open
```

Or run headless:

```bash
npx cypress run
```

> Make sure Node.js and npm are installed.
